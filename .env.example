# cp .env.example .env，填真实值。.env 已在 .gitignore，不会进仓库。
# scripts/run_server.sh 靠这个文件给 launchd 拉起的 server.py 提供密钥——
# launchd 的 plist 是明文 XML，密钥不适合直接写进去，所以走这个文件。
DOT_API_KEY=dot_xxx...
# DOT_DEVICE_ID=xxxxxxxx   # 可选：账号下只有一台设备时会自动发现
